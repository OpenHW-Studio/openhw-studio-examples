{
  "title": "RGB LED Serial Control Circuit Evaluation",
  "description": "Automatic evaluation criteria for RGB LED serial control circuit",
  "version": "2.0",
  "passingThreshold": 85,
  "evaluationCriteria": {
    "wiringAccuracy": {
      "weight": 0.43,
      "description": "Correct electrical connections between RGB LED and resistors",
      "requiredConnections": [
        {
          "from": { "component": "arduino", "pin": "9" },
          "to": { "component": "resistor", "terminal": "p1" }
        },
        {
          "from": { "component": "resistor", "terminal": "p2" },
          "to": { "component": "led", "terminal": "R" }
        },
        {
          "from": { "component": "arduino", "pin": "10" },
          "to": { "component": "resistor", "terminal": "p1" }
        },
        {
          "from": { "component": "resistor", "terminal": "p2" },
          "to": { "component": "led", "terminal": "G" }
        },
        {
          "from": { "component": "arduino", "pin": "11" },
          "to": { "component": "resistor", "terminal": "p1" }
        },
        {
          "from": { "component": "resistor", "terminal": "p2" },
          "to": { "component": "led", "terminal": "B" }
        },
        {
          "from": { "component": "led", "terminal": "COM" },
          "to": { "component": "arduino", "pin": "GND" }
        }
      ]
    },
    "codeFunctionality": {
      "weight": 0.37,
      "description": "Arduino code that reads serial commands and controls RGB LED",
      "requiredFunctions": ["pinMode", "digitalWrite", "Serial.begin", "Serial.readStringUntil"],
      "expectedBehavior": {
        "pinMode": "OUTPUT",
        "rgbPins": [9, 10, 11],
        "command": "color names via Serial",
        "baudRate": 9600
      }
    },
    "components": {
      "weight": 0.2,
      "description": "Required components for the circuit",
      "required": [
        { "type": "arduino", "count": 1 },
        { "type": "led", "count": 1 },
        { "type": "resistor", "count": 3 }
      ]
    }
  },
  "demoImages": [
    {
      "id": "circuit-layout",
      "name": "Correct Circuit Layout",
      "filename": "circuit.png",
      "description": "Expected RGB LED wiring with resistors"
    }
  ],
  "scoring": {
    "wiringAccuracy": {
      "excellent": { "min": 90, "feedback": "All RGB connections are correct!" },
      "good": { "min": 75, "feedback": "Most RGB connections are correct with minor issues" },
      "needsImprovement": { "min": 0, "feedback": "RGB wiring needs significant correction" }
    },
    "codeFunctionality": {
      "excellent": { "min": 90, "feedback": "Serial color control works perfectly!" },
      "good": { "min": 75, "feedback": "Serial control works but may need minor improvements" },
      "needsImprovement": { "min": 0, "feedback": "Serial color control needs significant work" }
    },
    "components": {
      "excellent": { "min": 90, "feedback": "All required components are present!" },
      "good": { "min": 75, "feedback": "Most required components are present." },
      "needsImprovement": { "min": 0, "feedback": "Missing one or more required components." }
    }
  }
}
