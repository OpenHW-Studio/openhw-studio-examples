{
  "title": "RGB LED Serial Control Circuit Evaluation",
  "description": "Automatic evaluation criteria for RGB LED controlled via Serial Monitor",
  "version": "2.0",
  "passingThreshold": 85,
  "evaluationCriteria": {
    "wiringAccuracy": {
      "weight": 0.43,
      "description": "Correct electrical connections between RGB LED, resistors, and Arduino",
      "requiredConnections": [
        {
          "from": { "component": "arduino", "pin": "9" },
          "to": { "component": "resistor", "terminal": "p2" }
        },
        {
          "from": { "component": "resistor", "terminal": "p1" },
          "to": { "component": "led", "terminal": "R" }
        },
        {
          "from": { "component": "arduino", "pin": "10" },
          "to": { "component": "resistor", "terminal": "p2" }
        },
        {
          "from": { "component": "resistor", "terminal": "p1" },
          "to": { "component": "led", "terminal": "G" }
        },
        {
          "from": { "component": "arduino", "pin": "11" },
          "to": { "component": "resistor", "terminal": "p2" }
        },
        {
          "from": { "component": "resistor", "terminal": "p1" },
          "to": { "component": "led", "terminal": "B" }
        },
        {
          "from": { "component": "led", "terminal": "COM" },
          "to": { "component": "arduino", "pin": "gnd_1" }
        }
      ]
    },
    "codeFunctionality": {
      "weight": 0.37,
      "description": "Arduino code that reads Serial commands and controls RGB LED colors",
      "requiredFunctions": [
        "pinMode",
        "digitalWrite",
        "Serial.begin",
        "Serial.available",
        "Serial.readStringUntil"
      ],
      "expectedBehavior": {
        "pinMode": "OUTPUT",
        "rgbPins": [9, 10, 11],
        "serialCommands": ["red", "green", "blue", "yellow", "cyan", "magenta", "white", "off"],
        "baudRate": 9600
      }
    },
    "components": {
      "weight": 0.2,
      "description": "Required components for the circuit",
      "required": [
        { "type": "arduino", "count": 1 },
        { "type": "led", "count": 1 },
        { "type": "resistor", "count": 3 }
      ]
    }
  },
  "demoImages": [
    {
      "id": "circuit-layout",
      "name": "Circuit Layout",
      "filename": "circuit.png",
      "description": "Expected RGB LED wiring with three 220Ω resistors"
    }
  ],
  "scoring": {
    "wiringAccuracy": {
      "excellent": { "min": 90, "feedback": "All RGB LED connections are correct!" },
      "good": { "min": 75, "feedback": "Most connections are correct with minor issues" },
      "needsImprovement": { "min": 0, "feedback": "RGB LED wiring needs significant correction" }
    },
    "codeFunctionality": {
      "excellent": { "min": 90, "feedback": "Serial RGB control works perfectly for all colors!" },
      "good": { "min": 75, "feedback": "Serial RGB control works but may need minor improvements" },
      "needsImprovement": { "min": 0, "feedback": "Serial RGB control needs significant work" }
    },
    "components": {
      "excellent": { "min": 90, "feedback": "All required components are present!" },
      "good": { "min": 75, "feedback": "Most required components are present." },
      "needsImprovement": { "min": 0, "feedback": "Missing one or more required components." }
    }
  }
}
